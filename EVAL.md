# DeepResearch Agent - 评测方案 (EVAL)

> 本文档定义评测集构造、指标定义与对照实验设计。
> **本项目简历上出现的每一个数字，都必须能由本文档描述的流程复现。**

---

## 1. 为什么要做评测

大多数个人 Agent 项目止步于"能跑出结果"，无法回答两个问题：

1. 你的优化到底有没有效果？
2. 效果有多大，是哪一项优化带来的？

评测层的存在，就是把"我做了混合检索"变成"混合检索让召回命中率从 A 提升到 B"。这也是本项目相对同类项目最主要的差异点。

面试中对应的高频追问是"你怎么知道效果变好了"，没有评测就只能回答"感觉上"。

---

## 2. 评测集设计

### 2.1 规模与构成

`eval/dataset/qa.jsonl`，共 **30 题**，分三类：

| 类型 | 数量 | 说明 | 主要考察 |
|------|------|------|----------|
| A 本地可答 | 12 | 答案完整存在于 `data/kb/` 语料中 | 检索层召回与重排质量 |
| B 需联网 | 10 | 语料中不存在，必须联网 | 降级策略是否正确触发 |
| C 混合 | 8 | 部分在本地、部分需联网 | 双通道协同与整合能力 |

### 2.2 单条样本格式

```json
{
  "id": "A-001",
  "type": "kb",
  "topic": "RRF 融合相比加权求和的优势是什么",
  "gold_chunks": ["hybrid_retrieval.md::3", "hybrid_retrieval.md::4"],
  "must_cover": ["排名而非分数", "免归一化"],
  "note": "语料中 hybrid_retrieval.md 第 3-4 切片直接描述"
}
```

| 字段 | 用途 |
|------|------|
| `type` | `kb` / `web` / `mixed`，决定适用哪些指标 |
| `gold_chunks` | **仅 A 类需要**，标注正确切片 id，用于计算召回命中率 |
| `must_cover` | 报告中应当出现的关键点，用于粗粒度覆盖度检查 |

### 2.3 构造原则

1. **语料先行**：先确定 `data/kb/` 语料，再基于语料出题，避免出无解的题
2. **gold_chunks 人工标注**：A 类的正确切片必须人工确认，这是召回指标可信的前提
3. **题目不含答案关键词**：避免题干与切片文本高度重合导致召回虚高（会让 BM25 通道被高估）
4. **固定不变**：评测集一旦定稿即冻结。中途改题会让前后实验数据不可比

> ⚠️ **诚实性约束**：`gold_chunks` 标注必须在跑实验**之前**完成。先跑结果再回头标注，等于让评测集迁就系统，指标失去意义。

---

## 3. 指标定义

### 3.1 检索质量（仅 A 类样本）

**召回命中率 Hit@K**

```
Hit@K = (至少召回一个 gold_chunk 的样本数) / (A 类样本总数)
```

**平均倒数排名 MRR**

```
MRR = (1/N) * Σ  1 / rank_of_first_gold_chunk
```

> Hit@K 回答"能不能找到"，MRR 回答"找到的排在多前"。重排优化主要体现在 MRR 上，只看 Hit@K 会低估重排价值。

### 3.2 生成质量（全部样本）

**引用可溯源率 Citation Groundedness**

```
可溯源率 = (报告引用编号能在 citations 中找到对应来源的数量) / (报告引用总数)
```

> 这是**防幻觉指标**。Writer 编造一个不存在的来源编号，此项立刻下降。因为 Writer 的引用编号被约束为与 `citations` 一一对应，该指标可完全程序化校验，无需人工。

**关键点覆盖率 Coverage**

```
覆盖率 = (报告中命中的 must_cover 关键点数) / (must_cover 总数)
```

> 使用关键词匹配 + 同义词表做粗粒度判定。**明确承认这是弱指标**，仅用于组间相对比较，不作为绝对质量结论。

**任务完成率 Completion Rate**

```
完成率 = (成功产出非空报告且无 fatal error 的样本数) / (总样本数)
```

### 3.3 效率与成本（全部样本）

| 指标 | 定义 |
|------|------|
| 平均端到端耗时 | 从任务开始到报告产出的墙钟时间均值 |
| 平均总 token | 单次任务全部 LLM 调用的 prompt + completion token 之和 |
| 平均成本 | 按 `core/costs.py` 价格表换算的单次任务费用 |
| P95 耗时 | 反映长尾，仅看均值会掩盖偶发超时 |

### 3.4 行为指标（v2 特有）

| 指标 | 定义 | 观察目的 |
|------|------|----------|
| 降级触发率 | 触发 `web_search` 降级的子问题占比 | 验证 `KB_SCORE_THRESHOLD` 是否合理 |
| 反思触发率 | `quality_score` 低于阈值而回退的任务占比 | 过高说明检索弱，过低说明 Critic 太宽松 |
| 反思增益 | 回退任务在返工后的分数提升均值 | **反思机制是否真的有用** |

> 反思增益是 Critic 模块存在价值的直接证据。如果增益接近 0，说明该机制只在烧 token，应在报告中如实写出并分析原因，而不是隐藏。

---

## 4. 对照实验设计

### 4.1 分组

四组递进配置，跑同一评测集，使每项优化的收益可**独立归因**：

| 组 | 配置 | 相对上一组的变化 |
|----|------|------------------|
| **A / baseline** | v1 行为：仅 web_search、串行、无 Critic、无重排 | — |
| **B / +混合检索** | 启用 kb_search（向量 + BM25 + RRF），关闭重排 | 检索层引入 |
| **C / +重排** | B 基础上启用 Rerank | 重排收益 |
| **D / 完整版** | C 基础上启用并行 + Critic 反思 | 编排层收益 |

### 4.2 控制变量

- 同一评测集、同一语料、同一模型、同一温度
- 同一时间窗内跑完，避免搜索结果随时间漂移
- 每组跑 **2 轮取均值**，降低 LLM 输出随机性影响
- 记录每组的完整配置快照到报告中

### 4.3 指标适用范围

| 指标 | A | B | C | D |
|------|---|---|---|---|
| Hit@K / MRR | ✗（无本地检索） | ✓ | ✓ | ✓ |
| 引用可溯源率 | ✓ | ✓ | ✓ | ✓ |
| 覆盖率 / 完成率 | ✓ | ✓ | ✓ | ✓ |
| 耗时 / token / 成本 | ✓ | ✓ | ✓ | ✓ |
| 反思触发率 / 增益 | ✗ | ✗ | ✗ | ✓ |

> A 组没有本地检索，因此 Hit@K 不适用。**不要用"A 组召回率为 0"这种误导性表述** —— 那是指标不适用，不是效果差。

---

## 5. 执行流程

```bash
# 1. 建库（一次即可）
uv run python -m rag.index_cli --dir data/kb

# 2. 分组跑评测
uv run python -m eval.runner --group baseline --rounds 2
uv run python -m eval.runner --group hybrid   --rounds 2
uv run python -m eval.runner --group rerank   --rounds 2
uv run python -m eval.runner --group full     --rounds 2

# 3. 生成对照报告
uv run python -m eval.report --out eval/reports/comparison.md
```

原始结果写入 `eval/reports/raw/{group}_{timestamp}.jsonl`，保留原始数据以便复核。

---

## 6. 报告格式

`eval/reports/comparison.md` 至少包含：

1. **实验元信息**：日期、模型、评测集版本、语料版本、各组配置快照
2. **主对照表**

| 指标 | A baseline | B +混合检索 | C +重排 | D 完整版 |
|------|-----------|------------|---------|---------|
| Hit@5 (A 类) | N/A | | | |
| MRR (A 类) | N/A | | | |
| 引用可溯源率 | | | | |
| 关键点覆盖率 | | | | |
| 任务完成率 | | | | |
| 平均耗时 (s) | | | | |
| P95 耗时 (s) | | | | |
| 平均 token | | | | |
| 平均成本 (元) | | | | |
| 反思触发率 | N/A | N/A | N/A | |
| 反思增益 | N/A | N/A | N/A | |

3. **归因分析**：每项优化带来的变化及其解释
4. **负面结果**：变差的指标必须写出来（例如反思机制必然抬高 token 与耗时）
5. **威胁与局限**（见下节）

---

## 7. 结论的边界（面试必答）

以下局限必须写进报告，并在面试中主动说明。**主动承认局限比被问出来强得多。**

| 局限 | 说明 |
|------|------|
| 样本量小 | 30 题不足以得到统计显著性，结论仅为方向性参考 |
| 覆盖率指标弱 | 关键词匹配无法判断语义正确性 |
| 评测集自建 | 出题人与开发者是同一人，存在选择偏差 |
| 联网结果不稳定 | B/C 类样本的搜索结果随时间变化，跨日期实验不可直接比较 |
| 无人工标注质量分 | 报告优劣未经第三方评价 |

> **对应的面试回答思路**：承认样本量与偏差问题，说明控制变量做法（同一评测集、同窗口、多轮取均值），并指出如果继续做会引入 LLM-as-judge 双盲评分与更大样本量。这个回答比"我提升了 30%"有说服力得多。

---

## 8. 简历数字的取用规则

1. 只取**组间对照**的数字，不取绝对值（"从 X 提升到 Y"比"达到 Y"更可信）
2. 数字后必须能立刻说出**归因**（是哪一项优化带来的）
3. 负面代价要能一并说出（例如"引入反思后质量提升，但 token 上升 N%，因此设了 2 轮硬上限"）
4. 报告中不存在的数字，简历上一律不写
