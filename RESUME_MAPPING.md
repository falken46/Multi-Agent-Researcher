# 项目 ↔ 简历 ↔ 面试 映射表

> **本文档的作用**：保证简历上写的每一句话，都能在仓库里指到具体文件、在评测报告里找到数据来源、并在面试中答得出"为什么这么做"。
>
> 使用方式：效果数字只从 `eval/reports/comparison.md` 取。R 轨已完成；P/Q 真实运行已转为可选。MCP Server 的代码、schema、项目配置与官方客户端 stdio 验证已完成；Phase 15 也已完成 Docker Compose 启动链和无 Key 的离线 CI 配置。本机已有 148 项离线测试、44 篇文档构建为 128 个 chunk、前后端健康检查通过等交付证据；GitHub Actions 远端绿色状态必须等作者提交并推送后再声明。
>
> ⚠️ **红线：占位符没填之前，这条 bullet 不许进简历。**

---

## 1. 项目一句话

**通用版（主投 AI Agent / AI 应用开发）**

> 基于 LangGraph 的多智能体深度研究系统，支持本地知识库与联网检索双通道，具备质量反思回环与断点续跑，并自带评测体系与全链路 trace。

**金融版** —— ⚠️ **已于 2026-09-09 移出本文档。**

金融方向现在由**独立项目**「证券材料合规审查 Agent」承担，代码 fork 自本项目，
其简历映射见 `实习材料/证券合规审查Agent/RESUME_MAPPING.md`。

> 🔴 **红线：两个项目不得重复声明同一份工作量。**
> 例如"210 项离线测试""C-MTEB 四组消融"是**本项目**的成果，合规版只能声明它自己新增的部分，
> 并在 README 中写明 fork 来源与迁移范围。两个仓库的 commit 历史、代码结构与相似度藏不住，
> 假装它们无关一戳就破；主动说出血缘反而是方法复用能力的证明。

---

## 2. 简历 bullet 草稿

### 2.1 通用版（4 条，建议全用）

```
多智能体深度研究系统（个人项目 · 开源）   Python · LangGraph · Milvus · PostgreSQL · FastAPI
- 基于 LangGraph StateGraph 编排 Planner / Researcher / Critic / Writer 四类 Agent，
  通过条件边实现质量不达标自动回退重研（硬上限 2 轮 + 定向补查 + 无增益退出三重保险），
  并接入 SqliteSaver 支持任务断点续跑。
- 自建检索层：递归切分 + 向量召回与 BM25 双通道 RRF 融合 + Rerank 重排，
  本地知识库优先、召回不足自动降级联网；在 100 题中文公开检索集（C-MTEB/T2Reranking）上
  做四组消融，发现混合检索是权衡而非普遍收益——MRR@5 +0.0538 但官方口径 MAP@20 -0.0059，
  据此把双通道与重排都做成可切换开关并记录适用边界，而非默认启用。
- 将向量库从 Chroma 迁移到 Milvus，用适配器窄接口隔离供应商差异，**上层 RRF 融合与重排代码
  一行未改**；以原语料重跑同一基准做 A/B 对照，R1/R2/R3 共 18 项指标与迁移前逐项一致
  （最大偏差 0.000045，即报告的四位小数精度），据此确认迁移未改变检索结果。
- 构建自动化评测与全链路 trace 体系，覆盖公开检索消融、P/Q 控制变量 runner、查询快照、
  断点续跑及 token / 成本汇总；并行与 Critic 的真实效果未实测，不填写推测数字。
- 用 PostgreSQL + SQLAlchemy + Alembic 落库研究任务、子问题、报告与引用来源，
  引用记录保存当时的检索名次以支持事后追溯；按 thread_id 幂等以适配断点续跑的重复收尾，
  并把落库设计成可关闭（未配置连接串则整层跳过），保住无外部依赖的离线回归。
- 基于官方 MCP Python SDK v2 暴露结构化 deep_research / kb_search 工具，提供项目级
  Claude Code 配置并通过官方客户端 stdio 握手与调用测试；用多阶段 Docker、Compose 启动门控和
  无 API Key 的 GitHub Actions 工作流完成交付，本机 210 项离线回归通过。
- 基于 OpenTelemetry 标准把事件流导出为带父子关系的 span（任务为根、Agent 节点为子、
  模型调用与降级为 span event），后端可指向任意 OTLP 接收端而无需改代码；
  本地 JSONL 保持为主路径，未配置 endpoint 时完全不介入。
```

### 2.2 金融版（4 条，换强调面）

```
多智能体研究报告生成系统（个人项目 · 开源）   Python · LangGraph · Chroma · FastAPI
- 设计四节点 Agent 流程并引入独立质量评审节点，对覆盖度、来源可溯源性与结论冲突
  三个维度打分，不达标自动返工（硬上限 2 轮），避免模型输出不可控地发散。
- 自建本地知识库检索层（切分 + 向量 / BM25 双通道 RRF 融合 + 重排），
  报告结论强制绑定来源编号并可逐条回溯到原始文档切片；在 100 题公开检索集上完成四组消融，
  发现首命中型指标在多正例数据上会失真，补齐官方口径 MAP@20 后如实保留了负向结论。
- 建立公开检索基准与 15 题端到端集的分轨评测流程，对四组检索配置做对照实验，
  量化每项优化的收益与代价；全链路 trace 记录逐次调用的模型、token、耗时与成本，
  任意一次输出均可审计还原。
- 工程化接口：以官方 MCP SDK v2 提供 deep_research / kb_search，参数和返回均生成
  JSON Schema，并用 stdio 客户端做协议级回归；以多阶段 Docker、自动建库的 Compose 启动链和
  无 API Key 的离线 CI 完成交付，本机 148 项回归通过。
```

### 2.3 版面不足时的压缩版（2 条）

```
- 基于 LangGraph 编排四类 Agent 并实现质量反思回环；自建向量 + BM25 混合检索与重排层，
  在 100 题公开检索集上做四组消融，用 MAP@20 / nDCG@5 复核并如实保留负向结论。
- 建立评测与 trace 体系记录检索、token、成本和行为事件，并以官方 MCP SDK v2
  暴露完整研究与本地检索工具；用 Docker Compose 与离线 CI 完成交付，148 项回归通过，
  未实测的并发收益和远端 CI 绿色状态不写入简历。
```

---

## 3. 模块 ↔ 简历 ↔ 代码 ↔ 数据 映射

| 简历表述 | 代码位置 | 数据来源 | 说不出来就不能写 |
|----------|----------|----------|------------------|
| 四类 Agent 编排 | `agents/graph.py`、`agents/critic.py` | — | 为什么要 Critic 独立成节点 |
| 反思回环 + 三重保险 | `agents/graph.py::should_revise` | trace revision 事件；P/Q 效果实测可选 | 怎么防死循环 |
| 断点续跑 | `agents/graph.py` Checkpointer | — | 状态存了什么、从哪恢复 |
| 递归切分 | `rag/splitter.py` | — | 中文切分和英文的差别 |
| 向量 + BM25 双通道 | `rag/vectorstore.py`、`rag/bm25.py` | R1 / R2 / R3 | 为什么要两路 |
| 向量库迁移与接口隔离 | `rag/vectorstore.py::MilvusVectorStore` / `create_vector_store`、`tests/test_rag_vectorstore.py` | Milvus 与 Chroma 在 R1/R2/R3 上 18 项指标一致，最大偏差 0.000045 | 为什么这点数据用不着 Milvus 也要换；Milvus 的 distance 和 Chroma 有什么区别 |
| 业务数据落库 | `db/models.py`、`db/repository.py`、`db/persistence.py` | 11 项 db 测试；`alembic upgrade/downgrade` 双向验证 | 为什么 citations 要存名次；落库失败为什么不中断主流程 |
| 数据库迁移 | `db/migrations/`、`alembic.ini` | 从空库 `upgrade head` 建全表、`downgrade base` 退回 | 为什么不用 create_all 当迁移方案 |
| OTel 导出 | `core/otel.py`、`tests/test_core_otel.py` | 12 项测试用官方 `InMemorySpanExporter` 走真实 SDK 读回 span | 事件流怎么映射成 span 树；为什么要显式指定父 span；**真实后端未连过，不能说"我用过 Langfuse"** |
| 增量关键词索引 | `rag/bm25.py::extend`、`tests/test_rag_bm25_incremental.py` | 8 项测试，含"增量与全量重建等价"及分词次数断言 | 为什么引擎必须重建、能省的只是分词 |
| 接口鉴权、调用主体与任务生命周期 | `backend/auth.py`、`backend/api.py`、`backend/streaming.py`、`frontend/app.py` | 17 项测试（401 / 404 / 503、前端请求头与 401 提示、actor 落库、`running/completed/failed`、恢复幂等） | 鉴权解决的是可观测不是安全；为什么前端只拿单个 key；为什么失败任务也必须落库 |
| RRF 融合 | `rag/hybrid.py` | R3 相对 R1 / R2 | 为什么不用加权求和 |
| Rerank 重排 | `rag/rerank.py` | R4 相对 R3（MAP@20 -0.0004，未观察到收益） | 重排和召回的区别 |
| 指标口径修订 | `eval/metrics.py`、`EVAL.md` §3.1 | Recall@5 / nDCG@5 / MAP@20 | 首命中型指标为何会失真 |
| 降级策略 | `agents/researcher.py` | trace `fallback` 事件 | 阈值怎么定的 |
| 引用可溯源率 | `agents/writer.py` + `eval/metrics.py` | 对照表 | 这个指标怎么算 |
| 并行改造 | `agents/researcher.py` | fake IO 并发边界测试；不声明真实加速数字 | 为什么用 asyncio 不用多线程 |
| token / 成本统计 | `core/llm.py`、`core/costs.py` | trace `task_end` | 钱花在哪个节点 |
| 评测体系 | `eval/` | `eval/reports/comparison.md` | 评测集怎么造的、有什么偏差 |
| MCP Server | `mcp_server/server.py`、`.mcp.json`、`tests/test_mcp_server.py` | 官方客户端 stdio 握手与调用结果 | MCP 和普通 API 的区别；为什么包名不能叫 `mcp` |
| CI / Docker | `.github/workflows/ci.yml`、`Dockerfile.*`、`docker-compose.yml` | 本机 210 项离线测试；v3 Compose 实跑完成 PostgreSQL 两版迁移、44 篇 → 128 chunk、Milvus/BM25 各 128、前后端 healthy + HTTP 200 | 为什么 CI 不放 API Key；为什么索引器要做启动门控；为什么迁移是独立的一次性服务；为什么中文路径要绕过 Bake |

---

## 4. 面试问题库与回答要点

> 按被问到的概率排序。每题的回答结构统一为：**做了什么 → 为什么这么选 → 代价是什么**。

### 4.1 编排层

**Q：为什么要加 Critic？直接让 Writer 自己检查不行吗？**
同一个节点既写又评，会倾向于给自己的产出打高分，评分就没有区分度了。分离之后 `quality_score` 才能当指标用，也才能作为条件边的判断依据。代价是每次任务多一次 LLM 调用。

**Q：反思循环怎么防止死循环？**
三重保险：① `revision_count` 硬上限 2 轮；② 返工时只针对 Critic 指出的 `missing_aspects` 定向补查，不重复全量检索，避免同样的输入得到同样的结果；③ 如果两轮后分数没有提升，直接放行到 Writer。另外技术失败重试 `retry_count` 和质量返工 `revision_count` 是分开计数的，不会互相挤占配额。

**Q：LangGraph 相比自己写调度好在哪？**
条件边和循环是一等公民，状态 schema 显式声明，加上官方 Checkpointer 能直接支持断点续跑和状态回放。如果手写调度，这些都要自己实现，而且流程一复杂就退化成一堆 if-else。

**Q：状态里为什么要区分 retry_count 和 revision_count？**
两者语义不同：前者是技术失败（超时、返回空），后者是质量不达标的主动返工。混用会导致一次网络抖动就吃掉返工配额，让反思机制失效。

### 4.2 检索层

**Q：为什么要混合检索，只用向量不行吗？**
理论上两路互补：向量擅长语义相近但用词不同，BM25 擅长专有名词、型号、缩写这类精确匹配。但我的实测数据**不支持把它说成普遍收益**，所以我讲的是权衡：在 100 题公开集上，RRF 相比单向量基线的顶部加权指标更好（MRR@5 +0.0538、nDCG@5 +0.0105），但覆盖类指标全面变差（Candidate Recall@20 -1.37、Hit@5 -1.00、Recall@5 -1.56 个百分点，官方口径 MAP@20 -0.0059）。机制是 Top-20 名额有限，而 BM25 单路本身较弱（MAP@20 只有 0.5538），融合时它把个别高相关 passage 顶到最前，同时把另一些相关 passage 挤出候选。所以我把双通道做成可切换开关，而不是默认打开。

> 补充：这个基准上单向量基线其实最强（Candidate Recall@20、Hit@5、Recall@5、MAP@20 四项最优）。这个结论对我不利，但我保留了它——如果只报 MRR@5 那一个提升，就是在多个指标里挑对自己有利的一个。

**Q：RRF 为什么优于加权求和？**
向量相似度和 BM25 分数量纲完全不同，加权融合必须先归一化，而归一化对分数分布敏感、换一批查询就要重调。RRF 只用排名不用绝对分数，无需调参，跨查询稳定。

**Q：Rerank 和召回有什么区别？**
召回追求“别漏”，用轻量方法从全库捞出候选；重排追求“排得准”，只能调整已经进入候选池的内容。我的 R3/R4 逐题复用同一 Top-20，所以 Candidate Recall@20 完全相同——这本身就是两者边界的直接证据。

结论是**当前 reranker 在这个基准上接近无效，而不是有害**：Recall@5 +0.38 个百分点、nDCG@5 -0.0104、MAP@20 -0.0004，基本持平。值得注意的是，我一开始只有 Hit@5 和 MRR@5，看到的是 -2.00 个百分点和 -0.0400，像是明显退化。后来发现这个数据集平均每个 query 有 7.55 个正例（100 query / 755 条 positive），首命中型指标只看第一个正例、在 93—96% 已经饱和，会夸大退化幅度；而 T2Reranking 在 C-MTEB 的官方主指标本来就是 MAP。补齐 MAP@20 之后，准确表述从“重排有害”修正成了“未观察到收益”。

**Q：那你为什么不干脆去掉重排？**
因为“在这个基准上无收益”不等于“在任何语料上无收益”。T2Reranking 是搜索日志式短 query 配网页片段，和本项目面向的技术文档知识库分布并不一致，而且向量基线在这里本来就很高、天花板有限。所以我保留可切换开关（`RERANK_BACKEND` 支持 onnx / llm / none），把结论限定在“已验证的范围”内，而不是外推成通用判断。

**Q：你这点数据用得着 Milvus 吗？**
用不着。本项目语料只有 44 篇文档、128 个切片，Chroma 嵌入式零运维完全够用；换 Milvus 多了三个容器（milvus + etcd + minio）和一份运维复杂度。我换它是为了走通企业级向量库的部署、schema 与索引配置，不是性能需要——**编一个性能理由反而更容易被追问穿**。所以 Chroma 分支我也留着，靠 `VECTOR_BACKEND` 切换。

**Q：那你怎么确认换库没把检索搞坏？**
做 A/B 对照，而且**先换库、不换语料**，把变量隔离开：两个后端各自建索引，跑同一套 100 题公开基准，R1/R2/R3 共 18 项指标逐项比对，结果完全一致（最大偏差 0.000045，就是报告四位小数的精度）。R2 是纯 BM25、不经过向量库，逐位相同，正好当对照组证明差异不来自评测流程本身的随机性。

**Q：迁移过程踩到什么坑？**
三个，都不是"不会用"，是"不知道它和上一个不一样"：
① **距离语义相反** —— Chroma 返回的是余弦*距离*（越小越相似），Milvus 在 COSINE 下返回的 `distance` **就是相似度本身**。照抄 `1 - distance` 不报错，只会把排序整个倒过来。
② **新连接里集合是 released 状态**，search 前要显式 `load_collection()`，否则报 `code=101`。这个坑单元测试抓不到——建集合的那个 client 是隐式加载的，只有"建索引"和"查询"分处两次运行才暴露，而那正是 pipeline 的真实用法。**它是 A/B 对照抓出来的，不是测试抓出来的。**
③ **环境变量 `MILVUS_URI` 和 pymilvus 自己撞名** —— 它在 import 时就读这个变量并强制按 http 解析，填本地文件路径会在 import 阶段崩，报错点离配置很远。项目侧改名成 `MILVUS_ENDPOINT`。

**Q：为什么 citations 要存检索名次？**
因为事后要能回答「当时为什么是这条排第一」。只存"引用了这个来源"的话，索引一重建、模型一升级，这个问题就永远没答案了。名次是**当时那次检索的事实**，必须和结论一起冻结。分数上游暂时没带下来，取不到就留空——填 0 会被误读成"相关度为零"。

**Q：落库失败了怎么办？**
只记日志，不打断主流程（fail-open）。研究助手丢一条任务记录不是灾难，但因为写库失败让用户拿不到整份报告是不可接受的。**但这个选择是场景相关的**：同样一层代码，放到合规审查场景就该反过来做成 fail-closed——一份没留痕的合规结论比报错危险得多。

**Q：embedding 模型怎么选的？**
用 fastembed 跑 ONNX 的 bge-small-zh。选它主要是**不依赖 torch** —— sentence-transformers 会把 torch 拖进来，Docker 镜像直接上 GB 级，而 ONNX 方案能压到百 MB 级，CPU 推理速度也够。代价是可选模型没有 HuggingFace 生态那么多。

**Q：降级阈值怎么定的？**
先跑评测集看本地可答题和需联网题的 `max_score` 分布，取两个分布的分界处。这个阈值偏高会把能本地回答的问题推去联网（贵且慢），偏低会拿不相关的本地内容凑答案（更糟）。trace 里记录了每次降级和当时的分数，可以复核。

### 4.3 评测与成本

**Q：你怎么知道优化真的有效？**
我把评测拆成三条独立对照。R 轨用 100 个公开 query 和 1,664 个共享 passage，R1/R2/R3/R4 分别只改变向量、BM25、RRF、rerank，确定性运行；P 轨只切换并发，Q 轨只切换 Critic，不与检索混在一条因果链。

更重要的是我还验证了**指标本身选得对不对**。最初三个指标全是首命中型，在多正例数据上会饱和，于是补了 Recall@5、nDCG@5 和官方口径 MAP@20 重判。补测时用同一配置重跑，旧的三列数值完全复现，确认新增指标没有改变数据基础，只是改变了能看见的东西。最后结论是混合检索和重排都没有普遍收益——这个结果对我不利，但它是真的，我把它留在了报告里。P/Q 真实运行已转为可选，没有 raw 就不会把并发或 Critic 数字写进简历。

**Q：评测集怎么造的？有什么问题？**
检索轨没有自己出题：从 `C-MTEB/T2Reranking` 固定抽取 100 个 query，把公开 positive 和 hard negative 合成 1,664 passage 的共享池，gold 直接来自 qrels。它避免了原来 20 个 chunk、Top20 等于全库的问题。局限是只抽了公开集子集，而且把 reranking 数据改造成共享池检索，结果不等于完整 T2Ranking 排行；P/Q 的 15 题仍是自建题集，存在选择偏差，关键词覆盖也只是弱指标，所以真实运行被调整为可选。

**Q：引用可溯源率是什么？怎么算的？**
报告里每个引用编号，能不能在传给 Writer 的 citations 里找到对应来源。因为我把 Writer 的引用编号约束成与 citations 严格一一对应，这个指标可以完全程序化校验，不需要人工。它本质是个**防幻觉指标** —— Writer 一旦编造来源编号，这个数字立刻掉下来。

**Q：一次任务成本多少？瓶颈在哪？**
trace 里逐次调用记了 token、耗时和成本，聚合后能看到节点分布。但当前没有执行正式 P/Q 付费任务，所以不对外声称平均成本或端到端瓶颈数字；已有能力是让下一次真实运行可以从 trace 直接回答，而不是从日志估算。

**Q：反思机制的代价？**
每次返工都会增加检索与 LLM 调用，因此设了 2 轮硬上限，并且用定向补查而不是全量重查来控制成本。正式 P/Q 尚未运行，所以只说明机制代价，不虚构 token 上升百分比。

### 4.4 工程

**Q：为什么用 asyncio 不用多线程？**
子问题之间完全独立，且瓶颈是网络 IO 不是 CPU，asyncio 收益最大，也能和 FastAPI 的异步栈自然配合。用 `Semaphore` 控制并发上限防止触发搜索 API 限流，`return_exceptions=True` 保证单个子问题失败不炸整体。

**Q：MCP Server 和普通 HTTP API 有什么区别？**
MCP 面向 LLM 客户端标准化了工具的描述与发现方式，客户端统一管理连接生命周期和权限，同一个 Server 可以被任何兼容客户端直接接入。普通 HTTP API 的契约由业务方各自定义，每接一个客户端就要单独适配。我实习时是 MCP 工具的调用方，这个项目里补上了 Server 端的实现。

**Q：CI 里为什么不放 API Key？**
CI 应该验证的是代码逻辑，不是外部服务可用性。所以需要真实调用的测试都打了 `live` 标记默认跳过，检索层测试用确定性的 fake embedding 后端。好处是 CI 快、稳定、且 fork 的人不用配 Key 也能跑。

**Q：为什么 Docker 要用多阶段构建和非 root 用户？**
构建阶段负责安装 `uv`、按锁文件解析依赖，运行阶段只复制虚拟环境与必要源码，避免把构建工具和下载缓存带进最终镜像；非 root 运行则把容器被利用后的权限边界收窄。代价是当前前后端仍复用完整运行依赖，两个镜像都约 324 MB。对秋招 Demo，我优先保证一个锁文件和可重复构建；如果进入生产，再拆 frontend/backend dependency group。

**Q：Compose 为什么要单独放一个 indexer，而不是后端启动时顺手建库？**
建索引是一次性初始化任务，后端是长期服务，两者生命周期不同。把 indexer 设成独立服务后，可以用“indexer 成功 → backend 启动 → `/health` 通过 → frontend 启动”的依赖链明确失败位置，也避免多个后端副本同时重建索引。当前 44 篇小语料每次启动全量重建，换取语料、切分配置与索引始终一致；代价是启动时间更长，后续数据量增大时应改成增量索引。

**Q：你说做了 CI，为什么仓库还没有绿色徽章？**
我已经完成工作流配置，并在本机跑通与 CI 完全相同的锁文件、Ruff 和 `pytest -m "not live"` 命令；但 GitHub Actions 只有在作者提交并推送后才会产生远端 run。现阶段可以说“实现了无 Key 的离线 CI”，不能说“远端 CI 已绿色”。推送后我会把具体 run 作为最终证据。

**Q：这个项目最大的不足是什么？**
公开检索只固定抽取了 T2Reranking 的 100 题子集，而且把 reranking 数据改造成共享池检索，不能外推成完整榜单结论；P/Q 没有付费实测，因此没有真实并发加速和 Critic 收益数字；知识库也还不支持增量更新，改一篇文档要全量重建索引。这三点分别限制了结论外推、端到端证据和部署规模。

> 最后这题几乎必问。**准备一个真实的、有技术含量的不足**，比说"时间不够"或"没什么不足"强得多。

---

## 5. 技能栏改写建议

现简历技能栏偏"使用者视角"，补上"构建者视角"后：

```
编程与数据：Python、SQL、PostgreSQL、SQLAlchemy ORM、Alembic 数据库迁移、
             asyncio 异步编程、数据清洗与 ETL、日志解析、pytest
AI 框架与 Agent：LangGraph 多智能体编排、状态机与条件边、Function / Tool Calling、
                 MCP（工具调用 + Server 开发）、结构化输出、反思与重试机制
RAG 与检索：文档切分、Embedding、向量检索（Milvus / Chroma）、BM25、RRF 混合召回、Rerank 重排、
            向量库迁移与适配器隔离、增量索引、检索评测（Candidate Recall / Recall@K / MRR / nDCG / MAP）
工程与可观测：FastAPI、SSE 流式、API Key 鉴权、Docker Compose、GitHub Actions CI、
              OpenTelemetry 埋点与 OTLP 导出、全链路 Trace、Token 与成本统计、固定测试集与回归验证
```

> 金融方向的技能栏见合规审查 Agent 项目的同名文档；两边描述同一项能力时，不要各写一遍当成两份经历。

---

## 6. 与实习经历的分工（面试时的叙事主线）

| | 实习（上海喆塔） | 本项目 |
|---|---|---|
| 角色 | 在企业 Agent 平台上开发 Skill | 从零搭建 Agent 系统底层 |
| 覆盖 | 业务场景路由、Prompt 工程、MCP 调用方、受约束 SQL、真实数据验证 | 框架编排、检索底层、评测工程、可观测、MCP 生产方 |
| 证明 | **业务可信度**：真实企业、真实数据、交付客户测试 | **技术可信度**：自己写的每一层都能讲清取舍 |

**串起来的一句话**（面试开场自我介绍可直接用）：

> 在实习里我把 Agent 落到了真实生产场景，处理的是业务规则、数据护栏和客户验收；在个人项目里我把 Agent 的底层自己写了一遍，包括编排、检索、评测和可观测。前者让我知道什么样的 AI 才能交付，后者让我知道它是怎么运转的。

---

## 7. 红线清单

写进简历前逐条自查：

- [ ] 每个效果数字都能在 `eval/reports/comparison.md` 或 trace 中找到；测试数和容器证据能由对应命令复现
- [ ] 每个技术名词在仓库里都有对应实现（Milvus / PostgreSQL / Alembic / OpenTelemetry 已实现可写；**不写 LangSmith、不写 Elasticsearch、不写 Kubernetes**）
- [ ] **不写"我用过 Langfuse / Laminar"** —— 只接了 OTel 标准，没有连过任何真实后端实例
- [ ] 每条 bullet 都能回答"为什么这么做"和"代价是什么"
- [ ] 不写"精通""深入理解""熟悉底层原理"
- [ ] 不写没跑成功过的能力
- [ ] GitHub Actions 没有真实远端 run 前，不写“CI 绿色”或放绿色徽章
- [ ] 项目与实习的表述不重复，各自承担不同的说服职能
- [ ] **本项目与合规审查 Agent 不重复声明同一份工作量**；合规版 README 写明 fork 来源与迁移范围
- [x] v3 Compose 本机启动链已验证：PostgreSQL 迁移、Milvus/BM25 建库、前后端健康均有真实日志
- [ ] 本机已验证真实 PostgreSQL 迁移与查询入口；同一套 db 测试的远端 CI 尚未运行，不声明 CI 已通过
