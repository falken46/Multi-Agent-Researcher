# 项目 ↔ 简历 ↔ 面试 映射表

> **本文档的作用**：保证简历上写的每一句话，都能在仓库里指到具体文件、在评测报告里找到数据来源、并在面试中答得出"为什么这么做"。
>
> 使用方式：只从 `eval/reports/comparison.md` 取已经跑出的数字。R 轨已完成；O 轨、MCP、CI 与 Docker 仍保留占位或不写进当前简历。
>
> ⚠️ **红线：占位符没填之前，这条 bullet 不许进简历。**

---

## 1. 项目一句话

**通用版（主投 AI Agent / AI 应用开发）**

> 基于 LangGraph 的多智能体深度研究系统，支持本地知识库与联网检索双通道，具备质量反思回环与断点续跑，并自带评测体系与全链路 trace。

**金融版（证券 / 金融 AI 应用开发）**

> 面向研究报告场景的多智能体系统，输出结论强制绑定来源、可逐条溯源，并通过固定评测集做回归验证，全链路调用可审计。

> 两版描述的是**同一个系统**，差别只在强调面：通用版讲技术纵深，金融版讲**输出可控、可溯源、可审计**——这正是券商 AI 岗最关心的点，也与实习中"白名单 + 只读 SQL 三层护栏"是同一套思维。

---

## 2. 简历 bullet 草稿

### 2.1 通用版（4 条，建议全用）

```
多智能体深度研究系统（个人项目 · 开源）   Python · LangGraph · Chroma · FastAPI
- 基于 LangGraph StateGraph 编排 Planner / Researcher / Critic / Writer 四类 Agent，
  通过条件边实现质量不达标自动回退重研（硬上限 2 轮 + 定向补查 + 无增益退出三重保险），
  并接入 SqliteSaver 支持任务断点续跑。
- 自建检索层：递归切分 + 向量召回与 BM25 双通道 RRF 融合 + Rerank 重排，
  本地知识库优先、召回不足自动降级联网；在 100 题中文公开检索集上，
  RRF 将 MRR@5 从 0.7238 提升至 0.7777，并通过消融发现当前 ONNX rerank 降至 0.7377，
  据此保留可切换开关而不把重排设为默认收益项。
- 构建自动化评测与全链路 trace 体系，量化引用可溯源率、任务完成率、token 与成本；
  子问题 asyncio 并行改造后端到端耗时下降 __%，单次任务平均 token 下降 __%。
- 以 MCP Server 形式对外暴露 deep_research / kb_search 能力，可被 Claude Code 直接调用；
  pytest 覆盖检索层与图编排共 __ 条用例，GitHub Actions CI + Docker Compose 一键部署。
```

### 2.2 金融版（4 条，换强调面）

```
多智能体研究报告生成系统（个人项目 · 开源）   Python · LangGraph · Chroma · FastAPI
- 设计四节点 Agent 流程并引入独立质量评审节点，对覆盖度、来源可溯源性与结论冲突
  三个维度打分，不达标自动返工（硬上限 2 轮），避免模型输出不可控地发散。
- 自建本地知识库检索层（切分 + 向量 / BM25 双通道 RRF 融合 + 重排），
  报告结论强制绑定来源编号并可逐条回溯到原始文档切片，
  在 100 题中文公开检索集上完成四组消融，RRF 的 MRR@5 达 0.7777。
- 建立公开检索基准与 15 题端到端集的分轨评测流程，对四组检索配置做对照实验，
  量化每项优化的收益与代价；全链路 trace 记录逐次调用的模型、token、耗时与成本，
  任意一次输出均可审计还原。
- 工程化交付：pytest __ 条用例、GitHub Actions CI、Docker Compose 一键部署，
  并提供 MCP Server 接口供外部客户端调用。
```

### 2.3 版面不足时的压缩版（2 条）

```
- 基于 LangGraph 编排四类 Agent 并实现质量反思回环；自建向量 + BM25 混合检索与重排层，
  100 题公开检索集上 RRF 将 MRR@5 从 0.7238 提升至 0.7777。
- 建立评测与 trace 体系量化每项改动的效果与代价，并行改造后耗时下降 __%；
  含 CI、Docker 与 MCP Server 接口。
```

---

## 3. 模块 ↔ 简历 ↔ 代码 ↔ 数据 映射

| 简历表述 | 代码位置 | 数据来源 | 说不出来就不能写 |
|----------|----------|----------|------------------|
| 四类 Agent 编排 | `agents/graph.py`、`agents/critic.py` | — | 为什么要 Critic 独立成节点 |
| 反思回环 + 三重保险 | `agents/graph.py::should_revise` | `EVAL.md` 反思触发率 / 增益 | 怎么防死循环 |
| 断点续跑 | `agents/graph.py` Checkpointer | — | 状态存了什么、从哪恢复 |
| 递归切分 | `rag/splitter.py` | — | 中文切分和英文的差别 |
| 向量 + BM25 双通道 | `rag/vectorstore.py`、`rag/bm25.py` | R1 / R2 / R3 | 为什么要两路 |
| RRF 融合 | `rag/hybrid.py` | R3 相对 R1 / R2 | 为什么不用加权求和 |
| Rerank 重排 | `rag/rerank.py` | R4 相对 R3（当前为负收益） | 重排和召回的区别 |
| 降级策略 | `agents/researcher.py` | trace `fallback` 事件 | 阈值怎么定的 |
| 引用可溯源率 | `agents/writer.py` + `eval/metrics.py` | 对照表 | 这个指标怎么算 |
| 并行改造 | `agents/researcher.py` | 对照表 D 组耗时 | 为什么用 asyncio 不用多线程 |
| token / 成本统计 | `core/llm.py`、`core/costs.py` | trace `task_end` | 钱花在哪个节点 |
| 评测体系 | `eval/` | `eval/reports/comparison.md` | 评测集怎么造的、有什么偏差 |
| MCP Server | `mcp/server.py` | — | MCP 和普通 API 的区别 |
| CI / Docker | `.github/workflows/`、`Dockerfile.*` | — | CI 里为什么不放 API Key |

---

## 4. 面试问题库与回答要点

> 按被问到的概率排序。每题的回答结构统一为：**做了什么 → 为什么这么选 → 代价是什么**。

### 4.1 编排层

**Q：为什么要加 Critic？直接让 Writer 自己检查不行吗？**
同一个节点既写又评，会倾向于给自己的产出打高分，评分就没有区分度了。分离之后 `quality_score` 才能当指标用，也才能作为条件边的判断依据。代价是每次任务多一次 LLM 调用。

**Q：反思循环怎么防止死循环？**
三重保险：① `revision_count` 硬上限 2 轮；② 返工时只针对 Critic 指出的 `missing_aspects` 定向补查，不重复全量检索，避免同样的输入得到同样的结果；③ 如果两轮后分数没有提升，直接放行到 Writer。另外技术失败重试 `retry_count` 和质量返工 `revision_count` 是分开计数的，不会互相挤占配额。

**Q：LangGraph 相比自己写调度好在哪？**
条件边和循环是一等公民，状态 schema 显式声明，加上官方 Checkpointer 能直接支持断点续跑和状态回放。如果手写调度，这些都要自己实现，而且流程一复杂就退化成一堆 if-else。

**Q：状态里为什么要区分 retry_count 和 revision_count？**
两者语义不同：前者是技术失败（超时、返回空），后者是质量不达标的主动返工。混用会导致一次网络抖动就吃掉返工配额，让反思机制失效。

### 4.2 检索层

**Q：为什么要混合检索，只用向量不行吗？**
向量召回擅长语义相近但用词不同的情况，但对专有名词、型号、缩写这类精确匹配容易失手；BM25 正好相反。理论上两路互补，但我没有把它简化成“全面提升”：100 题上 RRF 相比向量基线让 MRR@5 从 0.7238 升到 0.7777，同时 Candidate Recall@20 下降 1.37 个百分点。这说明融合改善了首个相关结果的位置，也会因 Top20 名额竞争丢掉部分向量候选。

**Q：RRF 为什么优于加权求和？**
向量相似度和 BM25 分数量纲完全不同，加权融合必须先归一化，而归一化对分数分布敏感、换一批查询就要重调。RRF 只用排名不用绝对分数，无需调参，跨查询稳定。

**Q：Rerank 和召回有什么区别？**
召回追求“别漏”，用轻量方法从全库捞出候选；重排追求“排得准”，只能调整已经进入候选池的内容。我的 R3/R4 逐题复用同一 Top20，所以 Candidate Recall@20 完全相同；但当前 ONNX reranker 让 Hit@5 从 95% 降到 93%、MRR@5 从 0.7777 降到 0.7377。这个负结果说明 reranker 与数据或候选分布并不匹配，因此我保留开关，没有把“用了重排”包装成“必然提升”。

**Q：为什么用 Chroma 不用 Milvus？**
数据量在万级切片以内，Chroma 嵌入式零运维就够了，Milvus 需要独立部署，收益不匹配复杂度。但我把向量库封装在 `vectorstore.py` 后面只暴露三个方法，要换 Milvus 只改这一个文件。

**Q：embedding 模型怎么选的？**
用 fastembed 跑 ONNX 的 bge-small-zh。选它主要是**不依赖 torch** —— sentence-transformers 会把 torch 拖进来，Docker 镜像直接上 GB 级，而 ONNX 方案能压到百 MB 级，CPU 推理速度也够。代价是可选模型没有 HuggingFace 生态那么多。

**Q：降级阈值怎么定的？**
先跑评测集看本地可答题和需联网题的 `max_score` 分布，取两个分布的分界处。这个阈值偏高会把能本地回答的问题推去联网（贵且慢），偏低会拿不相关的本地内容凑答案（更糟）。trace 里记录了每次降级和当时的分数，可以复核。

### 4.3 评测与成本

**Q：你怎么知道优化真的有效？**
我把评测拆成两轨。R 轨用 100 个公开 query 和 1,664 个共享 passage，R1/R2/R3/R4 分别只改变向量、BM25、RRF、rerank，确定性运行一轮；O 轨另测并行和 Critic，不与检索混在一条因果链。当前 R 轨已经跑完，既记录了 RRF 的 MRR 正收益，也保留了 rerank 的负收益；O 轨数字没跑完前不会写进简历。

**Q：评测集怎么造的？有什么问题？**
检索轨没有自己出题：从 `C-MTEB/T2Reranking` 固定抽取 100 个 query，把公开 positive 和 hard negative 合成 1,664 passage 的共享池，gold 直接来自 qrels。它避免了原来 20 个 chunk、Top20 等于全库的问题。局限是只抽了公开集子集，而且把 reranking 数据改造成共享池检索，结果不等于完整 T2Ranking 排行；O 轨的 15 题仍是自建题集，存在选择偏差，关键词覆盖也只是弱指标。

**Q：引用可溯源率是什么？怎么算的？**
报告里每个引用编号，能不能在传给 Writer 的 citations 里找到对应来源。因为我把 Writer 的引用编号约束成与 citations 严格一一对应，这个指标可以完全程序化校验，不需要人工。它本质是个**防幻觉指标** —— Writer 一旦编造来源编号，这个数字立刻掉下来。

**Q：一次任务成本多少？瓶颈在哪？**
trace 里逐次调用记了 token、耗时和成本，聚合后能看到节点分布。实测 Researcher 占了大部分耗时和 token，这也是我优先做并行化而不是先优化 prompt 的原因 —— 先看数据再决定优化哪里。

**Q：反思机制的代价？**
每次返工要多跑一轮检索加两次 LLM 调用，token 明显上升。所以设了 2 轮硬上限，并且用定向补查而不是全量重查来压成本。评测报告里这项负面代价是写出来的，没有藏。

### 4.4 工程

**Q：为什么用 asyncio 不用多线程？**
子问题之间完全独立，且瓶颈是网络 IO 不是 CPU，asyncio 收益最大，也能和 FastAPI 的异步栈自然配合。用 `Semaphore` 控制并发上限防止触发搜索 API 限流，`return_exceptions=True` 保证单个子问题失败不炸整体。

**Q：MCP Server 和普通 HTTP API 有什么区别？**
MCP 面向 LLM 客户端标准化了工具的描述与发现方式，客户端统一管理连接生命周期和权限，同一个 Server 可以被任何兼容客户端直接接入。普通 HTTP API 的契约由业务方各自定义，每接一个客户端就要单独适配。我实习时是 MCP 工具的调用方，这个项目里补上了 Server 端的实现。

**Q：CI 里为什么不放 API Key？**
CI 应该验证的是代码逻辑，不是外部服务可用性。所以需要真实调用的测试都打了 `live` 标记默认跳过，检索层测试用确定性的 fake embedding 后端。好处是 CI 快、稳定、且 fork 的人不用配 Key 也能跑。

**Q：这个项目最大的不足是什么？**
评测样本量偏小，结论只能算方向性参考；覆盖率指标是关键词匹配，判断不了语义正确性；另外知识库目前不支持增量更新，改一篇文档要全量重建索引。

> 最后这题几乎必问。**准备一个真实的、有技术含量的不足**，比说"时间不够"或"没什么不足"强得多。

---

## 5. 技能栏改写建议

现简历技能栏偏"使用者视角"，补上"构建者视角"后：

```
编程与数据：Python、SQL / MySQL、asyncio 异步编程、数据清洗与 ETL、日志解析、pytest
AI 框架与 Agent：LangGraph 多智能体编排、状态机与条件边、Function / Tool Calling、
                 MCP（工具调用 + Server 开发）、结构化输出、反思与重试机制
RAG 与检索：文档切分、Embedding、向量检索（Chroma）、BM25、RRF 混合召回、Rerank 重排、
            检索评测（Hit@K / MRR）
工程与可观测：FastAPI、SSE 流式、Docker Compose、GitHub Actions CI、
              全链路 Trace、Token 与成本统计、固定测试集与回归验证
```

> 金融版把顺序调整为：编程与数据 → 受约束查询与护栏 → RAG 与检索 → AI 框架与 Agent → 工程与可观测。

---

## 6. 与实习经历的分工（面试时的叙事主线）

| | 实习（上海喆塔） | 本项目 |
|---|---|---|
| 角色 | 在企业 Agent 平台上开发 Skill | 从零搭建 Agent 系统底层 |
| 覆盖 | 业务场景路由、Prompt 工程、MCP 调用方、受约束 SQL、真实数据验证 | 框架编排、检索底层、评测工程、可观测、MCP 生产方 |
| 证明 | **业务可信度**：真实企业、真实数据、交付客户测试 | **技术可信度**：自己写的每一层都能讲清取舍 |

**串起来的一句话**（面试开场自我介绍可直接用）：

> 在实习里我把 Agent 落到了真实生产场景，处理的是业务规则、数据护栏和客户验收；在个人项目里我把 Agent 的底层自己写了一遍，包括编排、检索、评测和可观测。前者让我知道什么样的 AI 才能交付，后者让我知道它是怎么运转的。

---

## 7. 红线清单

写进简历前逐条自查：

- [ ] 每个数字都能在 `eval/reports/comparison.md` 里找到
- [ ] 每个技术名词在仓库里都有对应实现（不写 Milvus、不写 LangSmith）
- [ ] 每条 bullet 都能回答"为什么这么做"和"代价是什么"
- [ ] 不写"精通""深入理解""熟悉底层原理"
- [ ] 不写没跑成功过的能力
- [ ] 项目与实习的表述不重复，各自承担不同的说服职能
