# DeepResearch Agent - 产品需求文档 (PRD v2)

> 版本：v2.0
> 上一版：v1.0（Planner → Researcher → Writer 三角色 Web 检索助手，已完成 Phase 0—9）
> 建议 GitHub 仓库名：`deepresearch-agent`

---

## 1. 版本说明：v1 → v2 变更概览

v1 已交付一个可用的三角色 Web 研究助手。v2 不推翻 v1，而是**在其之上补齐四个"深度"维度**。

| 维度 | v1 现状 | v2 目标 |
|------|---------|---------|
| 信息源 | 仅联网检索（Tavily） | **本地知识库 + 联网检索双通道**，本地优先、未命中降级 |
| 检索能力 | 直接使用搜索引擎返回的 snippet | **自建检索层**：切分 → 向量召回 + BM25 召回 → RRF 融合 → Rerank |
| Agent 编排 | 线性三节点 + 失败重试 | **四节点 + 质量反思回环**，子问题并行，支持断点续跑 |
| 质量证明 | 5 个主题人工跑通 | **自动化评测集 + 分组对照实验 + 量化指标报告** |
| 可观测性 | 终端日志 | **全链路 trace + token / 耗时 / 成本统计** |
| 对外接口 | HTTP API | 额外提供 **MCP Server**，可被 Claude Code 等客户端直接调用 |

---

## 2. 项目背景与定位

### 2.1 技术定位

DeepResearch Agent 是一个基于 LangGraph 的多智能体深度研究系统。用户输入研究主题，系统自动完成"主题拆解 → 多源检索 → 质量反思 → 报告撰写"的闭环，产出带来源引用的结构化 Markdown 报告。

区别于常见的"框架教程级 Demo"，本项目刻意把三个通常被省略的层做实：

1. **检索层自建**，而非直接使用搜索引擎或托管服务返回的现成结果
2. **质量可度量**，通过固定评测集与分组对照实验量化每一项优化的收益
3. **成本可观测**，每次任务的 token 消耗、耗时与费用可追溯到单次 LLM 调用

### 2.2 项目目标（求职语境）

本项目服务于 2027 届秋招 AI Agent / AI 应用开发方向投递，需要覆盖以下 JD 关键词簇：

| 关键词簇 | 由哪个模块承载 |
|----------|----------------|
| LangGraph、状态机、多智能体、条件边、反思机制 | 编排层 |
| 文档切分、Embedding、向量数据库、混合检索、Rerank | 检索层 |
| 评测集、指标定义、回归验证、对照实验 | 评测层 |
| Trace、Token 计量、成本优化、延迟优化 | 基础设施层 |
| MCP 协议（Server 开发） | 接口层 |
| 异步并发、SSE 流式、Docker、CI、pytest | 工程层 |

> **设计约束**：本项目必须与作者的企业实习经历**形成互补而非重叠**。实习经历已覆盖"业务场景路由 / Prompt 工程 / 受约束数据查询 / 真实数据验证"，因此本项目**不再重复应用层调优**，而聚焦于框架编排、检索底层、评测工程与系统工程化。

### 2.3 非目标（明确不做）

- 不做用户系统、登录与权限（单用户场景）
- 不做模型训练与微调
- 不做多租户与生产级高可用
- 不做超长报告（输出控制在 1500—3000 字）
- 不自研向量数据库（使用 Chroma，但融合与重排逻辑自建）
- 不接入付费 Rerank API（本地推理或 LLM Rerank 二选一）

---

## 3. 用户场景

### 3.1 场景 A：纯联网研究（v1 已支持）

用户输入「2026 年 AI Agent 领域的主要技术趋势」，系统联网检索并产出报告。

### 3.2 场景 B：基于私有资料的研究（v2 新增）

用户预先将一批 PDF / Markdown / 文本资料建入本地知识库（例如某领域论文、技术白皮书、项目文档），再提问。系统优先检索本地知识库，仅在本地召回不足时降级到联网检索，最终报告标注每条结论来自本地文档还是网络来源。

### 3.3 场景 C：作为 MCP 工具被外部 Agent 调用（v2 新增）

用户在 Claude Code 中配置本项目的 MCP Server，直接调用 `deep_research` 与 `kb_search` 两个工具，把深度研究能力嵌入日常工作流。

### 3.4 v2 工作流示意

```
用户输入主题
     |
[Planner]    拆解为 3-5 个子问题
     |
[Researcher] 并行处理各子问题
     |         |- kb_search   -> 本地知识库混合检索（向量 + BM25 -> RRF -> Rerank）
     |         |- web_search  -> 联网检索（本地召回不足时降级）
     |
[Critic]     质量评审：覆盖度 / 引用可溯源性 / 冲突检测
     |         |- 不合格 -> 回退 Researcher 补充检索（最多 2 轮）
     |
[Writer]     整合为带引用的 Markdown 报告
     |
前端展示 + 下载   ／   Trace 落盘（token / 耗时 / 成本）
```

---

## 4. 功能需求

### 4.1 P0 — 必须实现

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-01 | 文档入库流水线 | 支持 `.md` / `.txt` / `.pdf`，递归切分，写入向量库与 BM25 索引 |
| FR-02 | 向量检索 | 基于 Chroma，可插拔 Embedding 后端 |
| FR-03 | BM25 关键词检索 | 中文需分词，与向量检索并行执行 |
| FR-04 | RRF 混合融合 | 倒数排名融合，融合参数可配置 |
| FR-05 | 重排（Rerank） | 对融合后的候选做二次排序，取 Top-N 进入上下文 |
| FR-06 | `kb_search` 工具 | 将上述检索链路封装为 Researcher 可调用的工具 |
| FR-07 | 检索降级策略 | 本地召回分数低于阈值时自动降级到 `web_search` |
| FR-08 | Critic Agent | 对研究结果做质量评审，输出评分与改进意见 |
| FR-09 | 反思回环 | LangGraph 条件边，质量不达标回退 Researcher，硬上限 2 轮 |
| FR-10 | 子问题并行研究 | `asyncio.gather` 并发，带并发上限与超时 |
| FR-11 | 统一 LLM 客户端 | 集中封装重试、超时、token 计量 |
| FR-12 | Trace 记录 | 每次任务产生唯一 trace_id，逐次调用落盘 |
| FR-13 | 评测执行器 | 对固定评测集批量跑任务并计算指标 |
| FR-14 | 评测报告生成 | 输出分组对照的 Markdown 表格 |

### 4.2 P1 — 应当实现

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-15 | 断点续跑 | LangGraph Checkpointer（SQLite），任务中断后可从最近节点恢复 |
| FR-16 | 成本统计 | 按可配置的模型价格表换算单次任务费用 |
| FR-17 | 前端 Trace 面板 | Streamlit 展示本次任务的节点耗时与 token 分布 |
| FR-18 | MCP Server | 暴露 `deep_research` / `kb_search`，提供客户端配置示例 |
| FR-19 | Docker 化 | `docker-compose up` 一键启动前后端 |
| FR-20 | CI | GitHub Actions 运行 pytest |

### 4.3 P2 — 有余力再做

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-21 | 检索结果缓存 | 相同 query 命中缓存，降低重复调用成本 |
| FR-22 | 多轮对话式追问 | 基于已有报告继续提问 |
| FR-23 | 报告导出 PDF | |

### 4.4 工具集（v2）

| 工具名 | 用途 | 调用方 | 状态 |
|--------|------|--------|------|
| `web_search` | 关键词检索网页摘要 | Researcher | v1 已有 |
| `web_fetch` | 抓取指定 URL 完整内容 | Researcher | v1 已有 |
| `kb_search` | 本地知识库混合检索 | Researcher / MCP | **v2 新增** |

### 4.5 Agent 职责定义（v2）

#### Planner Agent（沿用 v1）

- 输入：用户研究主题
- 输出：3—5 个子问题（JSON）
- 策略：Few-shot + `response_format=json_object`

#### Researcher Agent（v2 改造）

- 输入：子问题列表
- 输出：每个子问题的资料摘要 + 结构化来源列表
- 变更点：
  1. 由串行改为 `asyncio.gather` 并行
  2. 先调 `kb_search`，召回不足时降级 `web_search`
  3. 来源从正文文本中剥离，改为结构化 `citations` 字段，使"引用可溯源率"可被程序校验

#### Critic Agent（v2 新增）

- 输入：主题、子问题、研究结果、来源
- 输出：`quality_score`（0—1）、`critique`（文字意见）、`missing_aspects`（需补查方向）
- 判定维度：
  1. **覆盖度** —— 子问题是否都有实质性内容
  2. **可溯源性** —— 关键论断是否有对应来源
  3. **冲突检测** —— 不同来源结论是否矛盾且未说明
- 约束：Critic 只评审不改写，改写由回退后的 Researcher 与 Writer 完成

#### Writer Agent（v2 微调）

- 输入：主题 + 子问题 + 研究结果 + citations
- 输出：Markdown 报告，正文使用角标引用，文末统一列参考来源
- 变更点：引用编号与 `citations` 一一对应，便于自动校验

---

## 5. 非功能需求

| 维度 | v1 要求 | v2 要求 |
|------|---------|---------|
| 端到端耗时 | 5 子问题 < 3 分钟 | **并行后目标 < 90 秒**（以评测实测为准） |
| 单任务成本 | 未度量 | **可度量并出具报告** |
| 错误恢复 | 单子问题失败不影响整体 | 同上，且失败原因写入 trace 与 errors |
| 并发控制 | 无 | 子问题并发上限可配置（默认 3），带单请求超时 |
| 可观测性 | 终端日志 | trace_id 贯穿全链路，逐调用记录模型、token、耗时、成本 |
| 可复现性 | 无 | 评测使用固定评测集与固定参数，结果可复现 |
| 部署 | Docker（未完成） | `docker-compose up` 一键启动 |
| 测试 | 41 条 pytest 通过 | 新增模块同等覆盖，CI 全绿 |

---

## 6. 验收标准

### 6.1 功能验收

- [ ] 能将一批本地文档建入知识库，并成功检索
- [ ] `kb_search` 返回结果包含文档来源、切片位置与相关性分数
- [ ] 混合检索各阶段（向量 / BM25 / RRF / Rerank）可单独测试
- [ ] 本地召回不足时能自动降级联网检索，且降级事件被记录
- [ ] Critic 能对低质量结果给出低分并触发回退，回退次数不超过硬上限
- [ ] 子问题并行执行，实测耗时显著低于串行
- [ ] 任务中断后可从 Checkpoint 恢复
- [ ] 每次任务产出完整 trace，含逐节点 token 与耗时
- [ ] 评测执行器能跑完整评测集并生成对照表
- [ ] MCP Server 能被 Claude Code 成功调用
- [ ] `docker-compose up` 在干净环境启动成功
- [ ] GitHub Actions CI 全绿

### 6.2 交付物验收

- [ ] `README.md` 含架构图、评测对照表、Demo 截图、技术决策说明
- [ ] `EVAL.md` 中的对照实验数据为真实跑出，非估计值
- [ ] `RESUME_MAPPING.md` 中每条简历表述都能对应到仓库中的具体实现
- [ ] 所有简历数字均可由 `eval/reports/` 下的报告复现

### 6.3 质量红线

> 以下任意一条不满足，该模块不得写入简历。

- 没有真实跑出数据的指标，不写
- 文档中出现但代码中不存在的技术名词，不写
- 依赖外部服务但从未实际调用成功的能力，不写

---

## 7. 里程碑

| 里程碑 | 内容 | 对应 Phase |
|--------|------|-----------|
| M1 可投最低线 | 检索层 + 编排升级跑通，README 可读，GitHub 上线 | Phase 10—12 |
| M2 完整线 | 评测报告 + trace 数据 + Docker + CI | Phase 13、15 |
| M3 加分线 | MCP Server + Demo 录制 + 技术复盘文章 | Phase 14、16 |

详细任务拆解见 `TASKS.md`。
