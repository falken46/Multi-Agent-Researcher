# DeepResearch Agent 中文面试口述稿

> 目标：把“我用了哪些技术”讲成“我遇到了什么问题、为什么这样设计、如何验证、边界是什么”。
>
> 数字纪律：检索效果只引用 `eval/reports/comparison.md`；P/Q 没有真实 raw，因此不声称并发加速比、Critic 质量提升率、真实任务成本或端到端完成率；GitHub Actions 尚未产生远端 run 前，不声称“CI 已绿色”。

## 1. 30 秒项目介绍

我做的是一个基于 LangGraph 的多智能体深度研究助手。Planner 拆解问题，Researcher 优先查本地知识库、召回不足时降级联网，Critic 从覆盖度、来源和冲突三个维度检查证据，Writer 最后生成带引用的 Markdown 报告。项目重点不是再包装一个聊天界面，而是把编排、混合检索、评测、trace、MCP、Docker 和 CI 串成一套可测试、可恢复、能说明技术取舍的工程系统。

## 2. 三分钟：LangGraph 编排与反思回环

### 可直接口述

我把流程拆成 Planner、Researcher、Critic、Writer 四个独立节点，用 LangGraph `StateGraph` 连接，而不是在一个函数里写 if-else。原因是这个任务本质上不是线性调用：Researcher 完成后，Critic 可能判定证据缺失，再通过条件边回到 Researcher。LangGraph 能把节点、共享状态和条件边显式化，后续加 checkpoint 也不需要自己重写一套调度器。

共享状态用 `ResearchState` 明确声明主题、子问题、证据、引用、质量分、错误、技术重试次数和质量返工次数。这里我特意把 `retry_count` 与 `revision_count` 分开：前者处理超时或空响应，后者表示内容质量不达标。如果混在一起，一次网络抖动就可能吃掉质量返工额度。

Critic 没有让 Writer 兼任。写作节点自己评价自己，分数容易失去区分度；独立 Critic 的输出既是可观测状态，也是条件边的路由依据。为了避免反思变成死循环，我做了三层限制：最多返工两轮；返工只查 Critic 给出的 `missing_aspects`，不重复全量研究；分数无增益时退出并交给 Writer。这样反思是受预算约束的控制流，不是“让模型多想几次”。

最后用 `SqliteSaver` 保存 LangGraph checkpoint，并用稳定的 `thread_id` 恢复同一任务。这个方案适合单机 Demo；如果多实例部署，SQLite 就不再合适，需要换成共享 checkpoint 存储。

### 追问抓手

- 代码证据：`agents/graph.py`、`agents/state.py`、`agents/critic.py`。
- 核心取舍：独立评审提高可控性，但增加一次 LLM 调用。
- 不夸大的边界：回环路由已确定性测试；Critic 的真实质量收益尚未付费实测。

## 3. 三分钟：RAG 检索流水线

### 可直接口述

检索层不是直接调用一个向量库，而是拆成文档加载、递归切分、embedding、Chroma 向量召回、BM25 关键词召回、RRF 融合和可选 rerank。向量召回解决语义相近但措辞不同的问题，BM25 对专有名词、型号和缩写更敏感。两路分数量纲不同，所以融合时没有直接加权求和，而用只依赖名次的 RRF，减少跨查询归一化和调参问题。

Rerank 和召回承担不同职责：召回先保证候选不要漏，rerank 只能对候选池重新排序，无法救回没被召回的文档。我把向量、BM25、融合和重排都做成可切换配置，因为“理论上互补”不等于在当前数据上一定提升。

这一点被真实评测验证了。在固定的 100 个中文公开 query 和 1,664 个共享 passage 上，我跑了 R1 到 R4 四组消融。RRF 相比单向量的 MRR@5 提升 0.0538、nDCG@5 提升 0.0105，但官方主口径 MAP@20 下降 0.0059；rerank 相比 RRF 的 MAP@20 又下降 0.0004，基本持平。因此我的结论不是“混合检索提升了效果”，而是它改善了顶部排序，却牺牲了一部分整体相关文档覆盖；当前单向量基线反而最稳。

线上策略仍保留本地知识库优先和低置信度联网降级。排序分与联网置信度分开，避免 rerank 改变排序的同时污染降级阈值。这里的局限是公开集来自搜索日志式短查询，和技术文档知识库分布不完全一致，所以结论只限定在这个基准，不外推成通用结论。

### 追问抓手

- 代码证据：`rag/pipeline.py`、`rag/hybrid.py`、`rag/rerank.py`、`tools/kb_search.py`。
- 为什么用 Chroma：当前是小规模、单机、嵌入式场景，零运维优先。
- 为什么保留无收益模块：保留可切换实验能力，不默认启用，也不把局部结果外推。

## 4. 三分钟：评测工程与“负向结果”

### 可直接口述

这个项目最重要的经验之一，是评测不能只为证明自己的方案有效。最初本地知识库只有很少的 chunk，`top_k` 接近全库大小，所谓召回实验实际上只是在排一个几乎没有干扰项的小列表。后来我接入 C-MTEB 的 T2Reranking 子集，固定抽取 100 个 query，把公开 positive 和 hard negative 合并成 1,664 个共享 passage，并直接用 qrels 作为 gold，这样 R1 到 R4 才有相同且有干扰项的被测对象。

我也修正过指标。最初只有 Hit@5 和 MRR@5，它们更关心第一个正例；这个数据集平均每题有 7.55 个正例，首命中指标在 93% 到 96% 区间容易饱和。后来补上 Candidate Recall@20、Recall@5、nDCG@5 和官方主指标 MAP@20，再用同一配置重跑，旧指标完全复现。新增指标没有改变原始数据，只是让我看见了“头部排序改善、整体覆盖下降”的真实权衡。

评测分成三条控制变量轨道：R 轨只比较检索组件，P 轨只切换串行与并发，Q 轨只切换 Critic。R 轨已经有 400 条结构化观测；P/Q runner 和续跑能力已经实现并测试，但我决定不为了简历数字紧急跑付费任务，所以没有 raw 就不报告并发加速或质量提升。这种保留负向结果和缺失数据边界的做法，比只挑一个好看的 MRR 数字更能说明我理解实验设计。

### 追问抓手

- 数据证据：`eval/reports/raw/`、`eval/reports/comparison.md`。
- 方法证据：`eval/metrics.py`、`eval/retrieval_runner.py`、`eval/report.py`。
- 局限：100 题是公开集子集；共享池改造不等于完整 T2Reranking 榜单成绩。

## 5. 三分钟：可观测、成本与恢复

### 可直接口述

所有 LLM 调用都收敛到 `core/llm.py` 的同步和异步入口，Agent 不能直接创建 OpenAI 客户端。统一入口负责模型调用、重试、token 读取、成本估算和 trace 事件。如果调用散落在各个 Agent，某一处漏记就会让任务成本和评测报告失真。

trace 使用 JSONL，一行一个结构化事件，包含 `trace_id`、节点、事件类型、耗时、token、估算成本，以及 fallback、revision、error、task_end 等行为事件。日志主要给人看，trace 给程序聚合；因此指标不会从日志文本里用正则抠。查询快照和评测 raw 也使用结构化文件，使失败后能按任务键续跑，而不是整批重来。

checkpoint 与 trace 解决的是两个不同问题：checkpoint 保存图运行状态，用于恢复执行；trace 保存发生过什么，用于审计和指标聚合。SSE 则把节点进度、降级、返工、用量和最终报告实时推给前端，它是展示通道，不作为评测数据源。

我现在不会回答“平均一次任务多少钱”，因为 P/Q 没有正式付费 raw。准确说法是系统已经具备逐调用计量和任务聚合能力，下一次真实运行可以从 trace 复算，而不是从终端日志估计。

### 追问抓手

- 代码证据：`core/llm.py`、`core/costs.py`、`core/trace.py`、`core/checkpoint.py`。
- 关键区分：日志面向排错；trace 面向可复算指标；checkpoint 面向恢复。
- 代价：集中 Gateway 成为关键路径，需要更严格的单元测试和降级处理。

## 6. 三分钟：MCP、Docker 与 CI 交付

### 可直接口述

系统对外有两类入口。FastAPI 加 SSE 服务 Web 前端；MCP Server 面向 LLM 客户端，暴露 `deep_research` 和只读 `kb_search` 两个结构化工具。MCP 的价值不只是换一种 RPC，而是让客户端能统一发现工具描述、输入输出 schema 和行为属性。我用官方 Python SDK v2 实现，并通过官方客户端启动真实 stdio 子进程，验证了握手、工具发现和 `kb_search` 调用。Claude Code 已能识别项目配置，但把工具结果发送给外部模型属于数据出站，因此我没有把它冒充成本地协议验收。

交付层有 backend、frontend 两个多阶段镜像，都按 `uv.lock` 安装依赖并以非 root 用户运行。Compose 另外定义一次性 indexer：先把 44 篇产品知识库构建成 128 个 chunk，确认 Chroma 与 BM25 各 128 条后再启动 backend；后端 `/health` 通过后才启动 frontend。索引、checkpoint、trace 和模型缓存都放命名卷。这样初始化失败会在正确阶段暴露，也避免服务在半成品索引上接流量。

CI 在 push 和 pull request 上执行锁文件同步、Ruff 与 `pytest -m "not live"`，不注入任何 API Key。真实网络能力不是靠普通 CI 验证，而是单独的 `live` smoke。本机同款命令目前是 148 项离线测试通过，Docker 干净命名卷启动链和两个健康检查也已验证。远端 GitHub Actions 要等我提交推送后才能说绿色，这个边界我会明确保留。

### 追问抓手

- 代码证据：`mcp_server/server.py`、`.mcp.json`、`Dockerfile.*`、`docker-compose.yml`、`.github/workflows/ci.yml`。
- 为什么多阶段：构建工具和缓存不进入运行镜像；当前两个镜像仍约 324 MB，后续可拆依赖组。
- 为什么不在 CI 放 Key：稳定、可复现、fork 安全；真实能力由显式 live 验证补充。

## 7. 一分钟：项目与实习如何互补

在实习里，我是在企业 Agent 平台上做 Skill，重点是业务场景路由、Prompt、受约束 SQL、数据护栏和客户验收，证明我能把 AI 功能放进真实业务。这个个人项目则刻意不重复应用层调优，而是从零补齐底层：LangGraph 编排、检索实现、评测设计、trace、MCP Server 和工程交付。前者证明业务可信度，后者证明技术纵深；两者共同说明我既知道 Agent 怎么运行，也知道什么样的 Agent 才能交付。

## 8. 高频追问的 20 秒回答

**为什么不用一个大 Agent？**

拆节点是为了让职责、状态变化和失败边界可观测；尤其 Critic 的输出要作为条件边依据，一个大 Prompt 很难独立测试和恢复。代价是调用次数与状态管理复杂度上升。

**为什么不用 LangSmith？**

我希望项目离线可运行且 trace 格式受自己控制，所以先用 JSONL。它满足当前审计和复算；团队协作、线上采样和可视化需求上来后，再考虑 LangSmith 或 OpenTelemetry。

**RRF 没有普遍收益，项目是不是失败了？**

不是。工程结论可以是“某技术在当前分布下不值得默认开启”。我用多指标发现它改善头部排序但损害覆盖，于是保留开关和适用边界，这比只报告正向指标更可靠。

**为什么不做 LLM-as-judge？**

当前优先验证确定性的检索和工程能力。LLM judge 会引入模型偏差、额外成本和复现问题；如果后续正式跑 P/Q，我会先固定 judge 模型、rubric 和盲评顺序，再报告结果。

**为什么每次 Compose 启动都重建索引？**

当前只有 44 篇小语料，全量构建成本可控，能保证索引与语料、切分配置一致。数据规模增大后再引入文档级版本和增量更新，否则首版会为尚未出现的问题增加复杂度。

**最大不足是什么？**

R 轨只有公开集的 100 题子集，P/Q 没有正式付费 raw，知识库也没有增量索引。因此我只报告确定性检索结果和交付证据，不把它包装成生产级效果结论。

## 9. 面试前自检

- 能在白纸上画出 `Planner → Researcher → Critic → Writer` 与返工条件边。
- 能解释 `retry_count`、`revision_count`、checkpoint、trace 四者各自解决什么问题。
- 能不看文档说出 RRF 的正负结果：MRR@5 `+0.0538`，nDCG@5 `+0.0105`，MAP@20 `-0.0059`。
- 能解释为什么多正例数据不能只看 Hit@5 / MRR@5。
- 能说清“已实现并测试”和“已证明真实收益”的区别。
- 能指出 148 项测试证明的是离线功能回归，不是真实模型准确率或联网稳定性。
- 能主动说明远端 CI、Claude Code 数据出站调用和 P/Q 付费评测仍待作者执行。
